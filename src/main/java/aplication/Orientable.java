package aplication;

public interface Orientable {
    Orientation orientation();

    Orientation orientation(Orientation orientation);

    default double angle(Orientation orientation) {
        return
    }
}
